# Base utilities and tools

Utilities in this directory are ubiquitous and are *not* allowed to have any internal dependencies on code outside this directory.

If you are looking to build or use utilities that depends on code outside the directory, please use [toolchain/util](../util).
