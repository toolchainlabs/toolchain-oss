# Devbox

Module to set up a developer box, useful for ssh-ing into for building code, docker images etc.

You SSH to the devbox via the bastion. E.g., `./bastion.sh` and then `ssh devbox1.toolchain.private`.

Your workdir on the box will be `/data/home/<yourusername>`.
