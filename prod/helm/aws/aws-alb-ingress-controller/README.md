# AWS ALB Ingress controller

Helm chart templates for running the AWS ALB ingress controller.

This controller intercepts requests to create generic Kubernetes Ingress resources,  and satisfies them by creating AWS Application Load Balancers (ALBs).
See <https://github.com/kubernetes-sigs/aws-load-balancer-controller>
